<!DOCTYPE html>
<html>
<head>
    <title>Products from Supermarket</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">

    <style>
        body {
            background-color: #f7f9fc;
        }

        .navbar-custom {
            background-color: #343a40;
            padding: 12px 20px;
        }

        .navbar-custom a {
            color: white !important;
            margin-right: 15px;
            font-weight: 500;
            text-decoration: none;
        }

        .page-container {
            max-width: 1100px;
            margin: 40px auto;
        }

        .product-card {
            background: white;
            padding: 16px;
            border-radius: 14px;
            box-shadow: 0px 4px 12px rgba(0,0,0,0.08);
            margin-bottom: 18px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .product-left {
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .product-image {
            width: 75px;
            height: 75px;
            object-fit: contain;
        }

        .product-name {
            font-size: 20px;
            font-weight: 600;
            margin: 0;
        }

        .qty-input {
            width: 65px;
            text-align: center;
        }

        .add-btn {
            background-color: #007bff;
            color: white;
            padding: 8px 16px;
            border-radius: 6px;
            font-weight: 500;
            border: none;
        }

        .add-btn:hover {
            background-color: #0062cc;
        }
    </style>
</head>

<body>

<!-- NAVBAR -->
<nav class="navbar-custom d-flex justify-content-between align-items-center">
   <div>
    <a href="/shopping">🏠 Home</a>
    <a href="/cart">🛒 View Cart</a>
    <a href="/orders">📦 Order History</a>
</div>

    <div>
        <span class="text-white">Welcome, <%= user.username %> (user)</span>
        <a href="/logout">🚪 Logout</a>
    </div>
</nav>

<div class="page-container">

    <h1 class="text-center mb-4">🛒 Products from Supermarket</h1>

    <!-- ✅ SEARCH BAR ADDED HERE -->
    <form method="GET" action="/shopping" class="mb-4" style="max-width: 400px; margin: 0 auto;">
        <input 
            type="text" 
            name="search" 
            placeholder="Search for products..." 
            class="form-control"
            value="<%= typeof search !== 'undefined' ? search : '' %>"
        >
    </form>
    <!-- END SEARCH BAR -->

    <% products.forEach(product => { %>
        <div class="product-card">
            
            <div class="product-left">
                <img src="/images/<%= product.image %>" class="product-image">

                <div>
                    <p class="product-name"><%= product.productName %></p>
                    <p class="text-muted small mb-0">
                        Stock: <strong><%= product.quantity %></strong>
                        &nbsp; | &nbsp;
                        Price: <strong>$<%= product.price.toFixed(2) %></strong>
                    </p>
                </div>
            </div>

            <form action="/add-to-cart/<%= product.id %>" method="POST" class="d-flex align-items-center gap-2">
                <input 
                    type="number" 
                    name="quantity" 
                    value="1" 
                    min="1" 
                    max="<%= product.quantity %>"
                    class="form-control qty-input">

                <button type="submit" class="add-btn">
                    Add to Cart
                </button>
            </form>
        </div>
    <% }) %>

</div>

</body>
</html>
