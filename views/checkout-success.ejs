<!DOCTYPE html>
<html>
<head>
    <title>Checkout Successful</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">

    <style>
        body {
            background-color: #f8f9fa;
        }

        .success-card {
            background: white;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            max-width: 700px;
            margin: 60px auto;
        }
        
         .navbar-custom {
            background-color: #343a40;
            padding: 12px 20px;
        }

        h1 {
            font-size: 32px;
            font-weight: 700;
            margin-bottom: 20px;
        }

        .emoji {
            font-size: 40px;
        }

        table {
            margin-top: 20px;
        }

        .btn-main {
            background-color: #28a745;
            color: white;
            font-weight: 600;
            padding: 10px 20px;
        }

        .btn-main:hover {
            background-color: #218838;
        }

        .back-link {
            margin-top: 20px;
            display: inline-block;
        }
    </style>
</head>

<body>
<nav class="navbar navbar-dark bg-dark px-3">
    <div>
        <a href="/shopping" class="navbar-brand">🏠 Home</a>
        <a href="/cart" class="navbar-brand">🛒 View Cart</a>
        <a href="/orders" class="navbar-brand">📦 Order History</a>
    </div>
    <div class="text-white">
        Welcome, <%= user.username %> (user) |
        <a href="/logout" class="text-white">Logout</a>
    </div>
</nav>

<div class="success-card">

    <h1><span class="emoji">🎉</span> Checkout Successful!</h1>

    <p>
        Thank you, <strong><%= user.username %></strong>!
        Your order has been processed. Here are the items you purchased:
    </p>

    <table class="table table-bordered text-center">
        <thead class="table-light">
            <tr>
                <th>Product</th>
                <th>Qty</th>
                <th>Price</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            <% items.forEach(item => { %>
            <tr>
                <td><%= item.productName %></td>
                <td><%= item.quantity %></td>
                <td>$<%= item.price.toFixed(2) %></td>
                <td>$<%= (item.quantity * item.price).toFixed(2) %></td>
            </tr>
            <% }) %>
        </tbody>
    </table>

    <form action="/download-pdf" method="POST">
    <input type="hidden" name="user[name]" value="<%= user.name %>">

    <% items.forEach(item => { %>
        <input type="hidden" name="items[]" value="<%= item.productName %>|<%= item.quantity %>|<%= item.price %>">
    <% }); %>

    <button type="submit" class="btn btn-danger">Download Receipt (PDF)</button>
</form>


    <a href="/shopping" class="back-link">← Back to Shopping</a>

</div>

</body>
</html>
